{
  "name": "demo",
  "version": "1.0.0",
  "author": "Alex Everitt",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "create-lambda": "./deploy.sh create",
    "update-lambda": "./deploy.sh update",
    "clear-build": "rm -rf lib/*",
    "build": "rm -rf lib/* && tsc",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "lint-fix": "tslint -c tslint.json 'src/**/*.ts' --fix"
  },
  "dependencies": {
    "@rotirojs/rotiro-claudiajs": "^1.0.0",
    "claudia": "^5.13.0",
    "claudia-api-builder": "^4.1.2",
    "rotiro": "^1.0.15"
  },
  "devDependencies": {
    "@types/jest": "^24.9.1",
    "@types/node": "^14.14.5",
    "prettier": "^1.19.1",
    "tslint": "^5.20.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.0.5"
  }
}
